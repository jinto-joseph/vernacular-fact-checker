# Core ML
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.1.0
joblib>=1.3.0

# API server
fastapi>=0.116.0
uvicorn[standard]>=0.35.0
pydantic>=2.0.0
python-multipart>=0.0.20

# Streamlit UI
streamlit>=1.35.0
requests>=2.31.0

# OCR — screenshots, infographics, WhatsApp forwards
pillow>=10.0.0
pytesseract>=0.3.10

# Deepfake / AI-generated face detection
# pillow already covers ELA + EXIF tamper detection (no extra package needed)
transformers>=4.40.0
torch>=2.2.0
